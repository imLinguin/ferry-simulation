#include "processes/passenger.h"

int main(int argc, char** argv) {

    // Handle control on one of 3 stations

    // Wait in the waiting room for gate to open

    // Board the gate through the ramp - VIPs have priority

    return 0;
}